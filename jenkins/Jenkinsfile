pipeline {
	agent none
    stages {

        stage('Build') {
			agent {
	        	docker {
	        	    image 'maven:3-alpine'
	        	    args '-u root'
		    	    }
		    }
            steps {
                sh 'mvn -B -DskipTests clean package'
                sh 'mvn test'
            }
		}
		stage('Build image') {
			agent{ label 'master'}
			steps {		
				sh "docker ps"
			}	
	  }
  }
}
